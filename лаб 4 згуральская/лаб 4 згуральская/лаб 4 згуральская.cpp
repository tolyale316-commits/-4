#include <stdio.h>

int main() {
    int N;

    printf("Введите число N: ");
    scanf("%d", &N);

    for (int i = 1; i = N; i++)
    {
        for (int j = N; j > N - i; j--)
        {
            printf("%d, j");
        }
        printf("\n");
        printf("Введите число N: ");
        printf("Введите число N: ");
    }
    return 0;
}